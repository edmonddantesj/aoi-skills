- 2026-03-14 09:47 KST — 장기 운영 원칙 / 로드맵 메모
  - 사용자의 장기 목표:
    - 초기부터 청묘팀/흑묘팀 포함 전체 운영을 제대로 체계화하고,
    - 자동화를 조금씩 만들어나가며,
    - 궁극적으로는 시스템이 자동으로 성장하고,
    - 일거리를 찾고,
    - 결과물을 내고,
    - 수익을 창출하며,
    - 사업을 확장해나가는 구조를 만들고 싶어함.
  - 방향성 해석:
    - 목표는 단순한 봇 수 증대가 아니라, 구조화/문서화/검색/인수인계/반복 실행 규격이 먼저 갖춰진 뒤 그 위에 리소스와 스쿼드를 확장하는 것.
    - 청묘팀/흑묘팀 자동화를 우선 완성하고, 이후 Claude Code 등 다양한 리소스를 붙여 중소기업급 자동 운영 구조로 확장하는 방향이 맞음.
  - 운영 원칙과의 정합성:
    - 현 시점에서는 무작정 별도 봇을 늘리기보다 현재 구조를 먼저 정교화한다.
    - 실제 병목이 확인되고, SSOT / handoff / shared search / Ralph Loop / human gate 설계가 충분히 성숙해진 뒤에만 점진적으로 확장한다.
  - 실질 로드맵:
    1. 현재 팀과 토픽 운영을 durable SSOT + shared search + handoff 기반으로 안정화
    2. 반복 가능한 내부 실행을 Ralph Loop / automation lane으로 이동
    3. maintenance / ops / hackathons / v6-invest 등 도메인별 자동화 고도화
    4. 이후 외부 리소스(Claude Code 등)와 추가 세션/스쿼드를 붙여 SMB급 운영 구조로 확장

- 2026-03-14 18:43 KST — AC/Judge 1차 적용 세트 확정
- Acceptance Criteria / Judge Pipeline / fail-only review는 active lane부터 점진 적용하는 방향으로 정리.
- 1차 적용 세트는 총 7개 토픽으로 확정:
  - hackathons
  - v6-invest
  - ops
  - inbox-dev
  - adp
  - ralph-loop
  - bazaar
- 해석:
  - 세 개만이 아니라, 현재 살아 있고 반복/산출물/판정/인수인계 가치가 큰 토픽부터 붙이는 것이 목적.
  - 특히 bazaar는 build/proof/scope/checkpoint/public-safe/admin/receipt/verify/archive 등 판정이 필요한 일이 많아 AC/Judge layer 효과가 큰 축으로 봄.
- 운영 방향:
  - 기존 STATUS / HANDOFF / one-line next 위에 Definition of Done / Acceptance Criteria / Judge rule 레이어를 얹는 식으로 적용.
  - 완료 판정 언어는 가능하면 `pass / fail / hold / needs-human-review`로 고정.

- 2026-03-14 19:48 KST — ADP AC/Judge 적용 방향 채택 후보로 정리
- 청정 제안 검토 결과, ADP에는 `office work card schema + judge/status UI + detail panel` 3종 세트로 AC/Judge를 붙이는 방향이 적절하다고 판단.
- 핵심 채택 포인트:
  - `state`와 `judgeResult` 분리 유지
  - 모든 카드에 one-line `Next:` 고정 노출
  - judge UI를 room-based workflow(approval / implementation / QA / validation / hold)와 연결
- 추가 보강 제안:
  - `updatedBy` 필드 추가
  - `proofLinks`는 `{ label, urlOrPath, kind }[]` 배열형 권장
  - `humanGate`는 `none / asset-approval / org-change / external-approval / publish-approval` enum 권장
- 단계 제안:
  - Phase 1: schema 확장 + STATUS/HANDOFF/NEXT/JUDGE 노출
  - Phase 2: DoD/AC/Judge rule/holdReason/humanGate 상세 패널
  - Phase 3: judgeResult 기반 room/queue/feed/state 전이 연결
- 해석: Topic 45(ADP)를 단순 activity 화면이 아니라 판정 가능한 작업 단위 + handoff 가능한 operating surface로 올리는 방향으로 봄.
